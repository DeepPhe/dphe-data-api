# Optional Compose env file. Use with:
# docker compose --env-file ./docker-compose.env up --build
#
# The local .env file is used automatically by Docker Compose and is the
# preferred place for machine-specific overrides.
DB_PATH=./test/resources/deepphe.sqlite3
# To serve the larger 500-patient fixture instead, use:
#DB_PATH=./test/resources/deepphe-500.sqlite3
PORT=3333
